# GenRAG Python dependencies — pip install -r requirements.txt

fastapi>=0.115.0          # web framework for REST API routes
uvicorn[standard]>=0.32.0 # runs FastAPI (uvicorn main:app --reload)
python-dotenv>=1.0.0      # loads .env (GEMINI_API_KEY)
openai>=1.55.0            # Gemini chat via OpenAI-compatible endpoint
pydantic>=2.0.0           # validates JSON request/response shapes
pypdf>=5.0.0              # PDF text extraction (Phase 2)
httpx>=0.27.0             # HTTP calls for Gemini embedding API (Phase 4)
python-multipart>=0.0.9   # required for file upload endpoints
chromadb>=0.5.0           # local vector database for embeddings
